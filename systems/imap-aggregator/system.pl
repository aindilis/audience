#!/usr/bin/perl -w

# get all the accounts we are supposed to watch

# look at contacts to judge importance

# aggregate into one mail thing with multiple directories perhaps,
# including (all received messages), most important, etc

# unified account

# accounts

# maybe do everything (IM, etc, all audience messages) through IMAP?
# I'm confused...  Could just try it though...
